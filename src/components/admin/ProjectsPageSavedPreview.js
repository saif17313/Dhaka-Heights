'use client';

import ProjectsPageClient from '@/components/ProjectsPageClient';

export default function ProjectsPageSavedPreview({ projectsPage }) {
  return <ProjectsPageClient projectsPage={projectsPage} />;
}
